# ============================================================
# L-Acoustics Professional Audio — Standard Library Templates
# LA4X and LA12X Amplified Controllers
# ============================================================

template LA4X {
  meta {
    kind: "device"
    manufacturer: "L-Acoustics"
    model: "LA4X"
    category: "Amplifier"
  }
  ports {
    Analog_In[1..4]: in(XLR) [analog, line_level]
    AES_In[1..2]: in(XLR) [AES3]
    Dante_Pri_In[1..4]: in(etherCON) [Dante, AES67, primary]
    Dante_Pri_Out[1..4]: out(etherCON) [Dante, AES67, primary]
    Dante_Sec_In[1..4]: in(etherCON) [Dante, AES67, secondary]
    Dante_Sec_Out[1..4]: out(etherCON) [Dante, AES67, secondary]
    Speaker_Out[1..4]: out(NL4) [speaker_level]
    Network_Control: io(etherCON)
    WordClock_In: in(BNC_75) [WordClock]
  }
  bridge Analog_In -> Speaker_Out
  bridge Dante_Pri_In -> Speaker_Out
}

template LA12X {
  meta {
    kind: "device"
    manufacturer: "L-Acoustics"
    model: "LA12X"
    category: "Amplifier"
  }
  ports {
    Analog_In[1..4]: in(XLR) [analog, line_level]
    AES_In[1..2]: in(XLR) [AES3]
    Dante_Pri_In[1..4]: in(etherCON) [Dante, AES67, primary]
    Dante_Pri_Out[1..4]: out(etherCON) [Dante, AES67, primary]
    Dante_Sec_In[1..4]: in(etherCON) [Dante, AES67, secondary]
    Dante_Sec_Out[1..4]: out(etherCON) [Dante, AES67, secondary]
    Speaker_Out[1..4]: out(NL4) [speaker_level]
    Network_Control: io(etherCON)
    WordClock_In: in(BNC_75) [WordClock]
  }
  bridge Analog_In -> Speaker_Out
  bridge Dante_Pri_In -> Speaker_Out
}

# --- P1: Processor and Measurement Platform -------------------

template P1 {
  meta {
    kind: "device"
    manufacturer: "L-Acoustics"
    model: "P1"
    category: "Loudspeaker Processor"
  }
  ports {
    Mic_In[1..4]: in(XLR) [Analogue, mic_level]
    Analog_In[1..4]: in(XLR) [Analogue, line_level]
    Analog_Out[1..4]: out(XLR) [Analogue, line_level]
    AES_In[1..4]: in(XLR) [AES3]
    AES_Out[1..4]: out(XLR) [AES3]
    AVB_Pri: io(etherCON) [AVB, Milan, primary]
    AVB_Sec: io(etherCON) [AVB, Milan, secondary]
  }
  bridge Mic_In -> AVB_Pri
  bridge Analog_In -> AVB_Pri
}
